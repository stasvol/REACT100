import {maxLength, minLength} from "./validator";

export const maxLength30 =  maxLength(30);
export const minLength2 =  minLength(2);
export const usersSearchFormValidate= () => {
    const errors = {};
    return errors;
}